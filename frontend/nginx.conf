server {
    listen 80;
    server_name staj.com;
    root /usr/share/nginx/html;

    # Debug logging
    error_log /var/log/nginx/error.log debug;
    access_log /var/log/nginx/access.log combined;

    # Serve the React app at /lib/
    location /lib/ {
        alias /usr/share/nginx/html/lib/;
        index index.html;
        try_files $uri $uri/ /lib/index.html;
        add_header X-Debug-Path $uri always;
    }

    # Redirect /lib to /lib/ (no trailing slash)
    location = /lib {
        return 301 /lib/;
    }

    # Proxy API requests
    location /lib/api/ {
        proxy_pass http://backend-service:3000/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        add_header X-Debug-Path $uri always;
    }

    # Redirect root to /lib/
    location = / {
        return 301 /lib/;
    }

    # Handle 404
    error_page 404 /lib/index.html;
} 